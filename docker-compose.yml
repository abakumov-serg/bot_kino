services:
  kino-bot:
    build: .
    container_name: kino-bot
    restart: unless-stopped
    env_file:
      - .env
    ports:
      - "${TELEGRAM_WEBHOOK_PORT:-8080}:${TELEGRAM_WEBHOOK_LISTEN_PORT:-8080}"
    volumes:
      - ./runtime:/app/runtime:ro
